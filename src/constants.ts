/**
 * Default listen port when LUMINA_PORT is unset.
 * Port is never configured via YAML — only via environment.
 */
export const DEFAULT_PORT = 3030;

/**
 * Default bind address when LUMINA_HOST is unset.
 * Host is never configured via YAML — only via environment.
 */
export const DEFAULT_HOST = "0.0.0.0";
